# For local HTTP use http://localhost. For public HTTPS use a DNS name that
# points to the server, for example monitor.example.com.
DOMAIN=http://localhost

# Optional primary FRED REST credential. Never commit the real value.
FRED_API_KEY=
