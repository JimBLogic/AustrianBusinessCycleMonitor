# Leave blank for same-origin requests.
# During local development, Vite proxies /api to http://localhost:5002.
# Set an absolute URL only when the frontend and backend are deployed separately.
VITE_API_URL=
