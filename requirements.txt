# Austrian Business Cycle Monitor - Ultimate Edition Requirements
# Production-ready dependencies for GitHub deployment

# Core Framework
flask>=2.0.0
flask-socketio>=5.0.0
flask-cors>=3.0.0  # Added to resolve missing module issue
gunicorn>=20.1.0
gevent>=21.12.0
waitress>=3.0.0
python-dotenv>=0.19.0

# Data Sources & APIs
fredapi>=0.5.0
requests>=2.25.0
pandas>=1.3.0
numpy>=1.20.0

# Visualization & Charts
matplotlib>=3.5.0
plotly>=5.0.0

# PDF Generation
reportlab>=4.0.0

# Environment & Configuration
python-dotenv>=0.19.0
pyyaml>=6.0

# Scientific Computing (Optional)
scipy>=1.9.0

# Web Scraping & Data Processing
beautifulsoup4>=4.11.0

# Cross-platform Terminal Support
colorama>=0.4.0

# Note: flask-babel removed - using custom JSON translation system
# This keeps the installation fast and lightweight
# Dev / Tooling (optional; install with `pip install -r requirements.txt -r dev-requirements.txt` if split later)